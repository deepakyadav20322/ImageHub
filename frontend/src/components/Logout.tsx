import React from 'react';


const Logout = ()=>{

    return(
        <>
        <p>We make logout with button logic in navigation page rather than /logout link</p>
        </>
    )
}

export default Logout